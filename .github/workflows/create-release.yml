# see: https://localheinz.com/articles/2022/01/24/creating-releases-with-github-actions/
name: "Create Release"

on:
  push:
    # Publish semver tags as releases.
    tags: [ 'v*.*.*' ]

jobs:
  release:
    permissions:
      contents: write

    uses: rwunderer/renovate/.github/workflows/reusable-create-release.yml@99603a54237793ef845fc3c438f373f1dfd350f8 # main
    secrets: inherit
