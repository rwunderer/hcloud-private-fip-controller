# see: https://localheinz.com/articles/2022/01/24/creating-releases-with-github-actions/
name: "Create Release"

on:
  push:
    # Publish semver tags as releases.
    tags: [ 'v*.*.*' ]

jobs:
  release:
    permissions:
      contents: write

    uses: rwunderer/renovate/.github/workflows/reusable-create-release.yml@605867cf6ee58c1d8e4380e797b5d6f358fc3a60 # main
    secrets: inherit
