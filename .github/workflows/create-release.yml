# see: https://localheinz.com/articles/2022/01/24/creating-releases-with-github-actions/
name: "Create Release"

on:
  push:
    # Publish semver tags as releases.
    tags: [ 'v*.*.*' ]

jobs:
  release:
    permissions:
      contents: write

    uses: rwunderer/renovate/.github/workflows/reusable-create-release.yml@9eb2183ddfa45262543b33ee0f6a768c8bb680c2 # main
    secrets: inherit
