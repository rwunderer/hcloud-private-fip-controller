# see: https://localheinz.com/articles/2022/01/24/creating-releases-with-github-actions/
name: "Create Release"

on:
  push:
    # Publish semver tags as releases.
    tags: [ 'v*.*.*' ]

jobs:
  release:
    permissions:
      contents: write

    uses: rwunderer/renovate/.github/workflows/reusable-create-release.yml@4648c9ad461b9867b4fb9b58f9efec5aff9794f9 # main
    secrets: inherit
